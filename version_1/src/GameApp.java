public class GameApp {
    public static void main(String[] args) {
        Game game = new Game();

        game.startGame();
        // game.setMark(game.getRow1(), 0, "X");
        // game.getBoard();
    }
}
